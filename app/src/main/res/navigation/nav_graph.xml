<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/startFragment">

    <fragment
        android:id="@+id/startFragment"
        android:name="hkbb.de.pflanzenmemorie.startFragment"
        android:label="fragment_start"
        tools:layout="@layout/fragment_start" >
        <action
            android:id="@+id/action_startFragment_to_fieldFragment"
            app:destination="@id/fieldFragment" />
    </fragment>
    <fragment
        android:id="@+id/fieldFragment"
        android:name="hkbb.de.pflanzenmemorie.fieldFragment"
        android:label="fragment_field"
        tools:layout="@layout/fragment_field" />
</navigation>